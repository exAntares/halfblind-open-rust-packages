name: Deploy Packages Manually

on:
  push:
    tags:
      - '[0-9]+.[0-9]+.[0-9]+'

jobs:
  upload-packages:
    uses: ./.github/workflows/upload-npm-packages.yml
    with:
      version: ${{ github.ref_name }}
    secrets:
      NPMRC_FILE_CONTENTS: ${{ secrets.NPMRC_FILE }}